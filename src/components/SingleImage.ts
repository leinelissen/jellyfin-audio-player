import FastImage from '@d11/react-native-fast-image';
import styled from 'styled-components/native';
// import FastImage from 'react-native-fast-image';

const SingleImage = styled(FastImage)`
    border-radius: 0px;
    width: 50px;
    height: 50px;
    margin-bottom: 0px;
`;

export default SingleImage;